{
  "name": "bela-film-co",
  "version": "1.0.0",
  "private": true,
  "description": "Static website for Bela Film Co.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css script.js dist/",
    "check": "node --check script.js"
  }
}
